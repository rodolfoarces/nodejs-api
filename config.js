const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "prueba_js",
      password: "prueba_js.",
      database: "prueba_js",
    },
    listPerPage: 10,
  };
  module.exports = config;